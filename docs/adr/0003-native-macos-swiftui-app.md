# Build a native macOS SwiftUI app

SkillSelector starts as a native macOS app built with SwiftUI and packaged as a `.dmg`. This choice supports the product goal of being small, memory-light, and aligned with macOS directory permission flows better than an Electron, Tauri, or browser-based shell.
